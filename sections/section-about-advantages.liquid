{{ 'section-about-advantages.css' | asset_url | stylesheet_tag }}

<style>
  .section-{{section.id}}{
      padding:{{section.settings.padding}};
  }
  .section-{{section.id}} .page-layout-content {
    padding-top: 85px;
    padding-bottom: 85px;
  }

  @media screen and (max-width: 768px) {
      .section-{{section.id}}{
      padding:{{section.settings.md_padding}};
  }
  }
</style>

<div class="page-layout section-{{section.id}}">
  <div class="page-layout-content">
   <div class="abadvan"> 
    <h2 class="section-abadvan-h2">{{ section.settings.title }}</h2>
   </div>
    <div class="abadvan-model">
      {% for block in section.blocks %}
      <div class="abadvan-item">
        <div class="abadvan-img">
         {% render 'img-lazyload', image_url: block.settings.img %}
        </div>
        <span class="abadvantitle">{{ block.settings.text }}</span>
      </div>
        {% endfor %}
    </div>
    
  </div>
</div>
  
{% schema %}
{
  "name": "section-about-abadvan",
  "settings": [
    
    {
      "type": "richtext",
      "id": "title",
      "label": "标题"
    }
  ],
  "blocks": [
    {
      "name": "item",
      "type": "item",
      "settings": [
        {
          "type": "image_picker",
          "id": "img",
          "label": "配图"
        },
        {
          "type": "richtext",
          "id": "text",
          "label": "文本内容"
        }
      ]
    }
  ],
  "presets": [
    {
      "name": "section-about-abadvan"
    }
  ]
}
{% endschema %}